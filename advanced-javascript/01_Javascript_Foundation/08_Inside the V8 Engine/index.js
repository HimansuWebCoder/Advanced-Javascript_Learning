// interpreter vs compiler

function someCalculation(x, y) {
	return x + y;
}

for (let i = 0; i < 1000; i++) {
	// compilers do this directly
	// 9;
}

// = X

// combine both and named JIT Compiler
// Just in time compiler (JIT)
