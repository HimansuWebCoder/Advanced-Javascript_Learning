// For the longest time, you had to resort to workarounds and libraries to create a deep copy of a
// javascript object. However, most browsers have a new structuredClone(),
// a built-in function for deep-copying!

// This is an interesting new feature which you can read
